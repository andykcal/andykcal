using System;

class Program {
  public static void Main (string[] args) {
    int num=10;
    if(num==10)
      Console.WriteLine("숫자가 10입니다.");
    else
      Console.WriteLine("숫자가 10이 아닙니다.");
  }
}
